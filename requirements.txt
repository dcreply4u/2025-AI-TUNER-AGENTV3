pyside6
pyqtgraph
obd
racecapture
numpy
pandas
openpyxl
pyarrow
scikit-learn
discord.py>=2.3.0
python-dotenv>=1.0.0
joblib
requests
SpeechRecognition
PyAudio
pyttsx3
pyserial
pyusb  # For USB device detection on Windows
pynmea2
opencv-python
psutil
cantools
python-can
paho-mqtt
cryptography
psycopg2-binary
pytest
pytest-cov
colorama
# Mobile API Server dependencies
fastapi
uvicorn[standard]
websockets>=11.0
aiohttp>=3.8.0
# Optional: YubiKey support (install separately if needed)
# yubikit

# Optional: Advanced ML features (for optimized AI algorithms)
# torch  # For LSTM models in optimized fault detector
scipy  # For advanced statistics and signal processing (Savitzky-Golay filter for dyno)
# Optional: Natural Language Processing (for conversational AI)
# nltk  # For text processing and tokenization (lightweight NLP)

# RAG AI Advisor dependencies (required for modern AI advisor)
chromadb>=0.4.0  # Vector database for semantic search
sentence-transformers>=2.2.0  # Embeddings for semantic search
ollama>=0.1.0  # Local LLM client (optional, install Ollama separately from https://ollama.ai)

# Knowledge Base Manager dependencies (for document ingestion and web scraping)
PyPDF2>=3.0.0  # PDF document parsing

# Waveshare Environmental Sensor HAT dependencies (optional)
# Install with: pip install adafruit-circuitpython-bme280 smbus2
# adafruit-circuitpython-bme280>=2.6.0  # BME280 sensor library (I2C)
# smbus2>=0.4.0  # I2C bus access (alternative to Adafruit)
python-docx>=1.0.0  # DOCX document parsing
beautifulsoup4>=4.12.0  # HTML parsing for web scraping
lxml>=4.9.0  # XML/HTML parser (faster than default)

# Google Search API (optional, for high-quality web search)
google-api-python-client>=2.100.0  # Google Custom Search API client
# Optional: For analog sensors (ADC boards)
# adafruit-circuitpython-ads1x15
# Optional: For GPIO/digital sensors (Raspberry Pi)
# RPi.GPIO
# Optional: For GPIO on Jetson
# Jetson.GPIO

